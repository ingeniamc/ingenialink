#ifndef PUBLIC_INGENIALINK_CONFIG_H_
#define PUBLIC_INGENIALINK_CONFIG_H_

/** Library version. */
#define IL_VERSION "${PROJECT_VERSION}"

/** Library is static. */
#cmakedefine IL_STATIC

/** Library is compiled for big-endian systems. */
#cmakedefine IL_BIG_ENDIAN

#endif
